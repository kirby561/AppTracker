<Window x:Class="AppTracker.LabelEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppTracker"
        mc:Ignorable="d"
        Title="Labels" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView Name="_labelListView" />

        <StackPanel Grid.Row="1" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button Margin="5" Padding="5,2,5,2" Content="New" Click="OnNewClicked" />
            <Button Margin="5" Padding="5,2,5,2" Content="Delete" Click="OnDeleteClicked" />
        </StackPanel>

        <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="5" Padding="5,2,5,2" Content="Done" Click="OnDoneClicked" />
        </StackPanel>
    </Grid>
</Window>
